

export const typeTotals = set => {
    // return obj with all types as keys except basic lands
}

export const setPriceAverager = set => {
    // return obj with total price average minus basics 
    // and average top ten price average.
}